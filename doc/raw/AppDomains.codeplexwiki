The default property connector instance is _per-AppDomain_. This means that the same names used in different AppDomains refer to different properties.

When an AppDomain is unloaded, all property values connected by the default property connector in that AppDomain become eligible for garbage collection.